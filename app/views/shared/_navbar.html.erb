<nav class="navbar navbar-light">
  <a class="navbar-brand" href="#">Thougthbox</a>
    <form class="form-inline pull-xs-right">
      <% if current_user %>
        <span><%= current_user.email %></span>
        <%= link_to 'Sign Out', logout_path, class:'btn btn-primary-outline' %>
      <% else %>
        <button class="btn btn-primary-outline" type="submit">Sign In/Sign Up</button>
      <% end %>
    </form>
</nav>
